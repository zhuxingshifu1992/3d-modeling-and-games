# 公开版验证

日期：2026-09-24。检查对象为本次脱敏后的公开副本，全部游戏自动验证使用 Godot 4.7.2 `--headless`，每个测试使用隔离的存档目录。

| 检查 | 结果 |
| --- | --- |
| Godot 首次资源导入与启动 | 通过，模型、贴图、字体和脚本可加载 |
| 升降台、存档、鼠标控制、窄入口及登舱对齐 | 通过；测试不会捕获或移动桌面鼠标 |
| 舱内家具、进出舱与精细机位净空 | 通过，包括元祖、ν、能天使、强袭自由与独角兽 |
| 六机完整往返 | 通过：步行上平台、升降、进出舱、自检、返回及存档重读；飞翼采用公开版程序模型 |
| 打包预检查 | 通过；本次提供源码与资产，不发布旧 EXE/PCK |
| 12 份 GLB | 926 组几何访问数据与原件一致，160 张图像仅清理元数据；独角兽 80 张图像外置后引用有效 |
| 3 份 Blender 工程 | 全部后台重新打开，几何数量一致，所用图像已打包并成功解码 |
| 发布文件 | 个人路径/标识、常见凭据、图片元数据、Python 语法、文档链接与 GitHub 单文件限制检查通过 |

游戏共完成 23 个步骤（资源导入 1 次，加 22 个运行验证），六机往返包含 100 项检查。运行记录见 [验证摘要](validation.json)。测试使用无窗口模式，不代表 GPU 画质或帧率验证；预览为已注明的历史 CPU 结构渲染。

源工程文件的大小、修改时间以及所复制原件的 SHA-256 均未变化。当前发布文件的完整性以仓库根目录 `MANIFEST.sha256` 为准，制作报告中记录的上游哈希保留历史意义。
